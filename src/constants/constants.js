const TABELS = {
  ADMIN: "library_admin",
  STUDENT: "library_students",
  BOOKS: "library_books"
};

const LABELS = {
  LOGIN_ERROR: "Username or password is incorrect please try again."
};

module.exports = {
  TABELS,
  LABELS
};
