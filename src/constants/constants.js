const TABLES = {
  ADMIN: "library_admin",
  STUDENTS: "library_students",
  BOOKS: "library_books"
};

const LABELS = {
  LOGIN_ERROR: "Username or password is incorrect please try again."
};

module.exports = {
  TABLES,
  LABELS
};
